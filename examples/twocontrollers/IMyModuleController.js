Aria.interfaceDefinition({
	$classpath : "examples.twocontrollers.IMyModuleController",
	$extends : 'aria.templates.IModuleCtrl',
	$interface : {
		/**
		 * Increment the value of the counter inside the data model.
		 */
		incrementCount : function () {}
	}
});